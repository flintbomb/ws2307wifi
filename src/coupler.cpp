// Coupler-specific page renderer was retired when the three K-1/K-2/K-3
// SVG gauges were folded into the control page. The /coup{1,2,3}.php
// routes and make_coupler() helper are gone. This file is intentionally
// empty so the build keeps a stable file list; remove it from the source
// tree if you'd like.

#include "ws2307.h"
